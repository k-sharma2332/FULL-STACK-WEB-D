// Take the user's name & age as input using prompts.
// Then return back the following statement to the user as an alert (by substituting thier name & age):
//                name is age years oldset 
let firstName = prompt("Enter name of the user");
let age = prompt("Enter the age of the user");
let msg = firstName + " " + "is" + " " + age + " " + "year oldest";
alert(msg);