// Qs3. Create a new array whose elements are in uppercase of words present in the
// original array.
let arr = ["rajat","khushi","rahul","sacchi"];
console.log (arr.map((arr) => arr.toUpperCase(arr)));
//console.log(newArr);